import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class TokenService {
  // No se necesita implementación
  // Se usa autenticación basada en sesión y cookies
}